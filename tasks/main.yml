---
# tasks file for derekpurdy.configure_prometheus_targets

- name: 'Validate Prometheus configuration directory exists'
  ansible.builtin.file:
    path: "{{ prometheus_config_dir }}"
    state: directory
    mode: "0755"
  tags:
    - prometheus
    - config

- name: 'Configure Prometheus yml file'
  ansible.builtin.template:
    src: "{{ prometheus_config_file }}"
    dest: "{{ prometheus_config_dir }}/prometheus.yml"
    mode: "0644"
    validate: "python3 -c 'import sys, yaml; yaml.safe_load(open(\"%s\"))'"
  notify:
    - 'Restart Prometheus docker container'
  tags:
    - prometheus
    - config
