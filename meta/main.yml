---

galaxy_info:
  role_name: configure_prometheus_unraid
  author: derekpurdy
  description: Configure prometheus.yml file on Unraid via Ansible using the Ansible hosts list
  license: MIT
  min_ansible_version: '2.10'
  platforms:
    - name: Ubuntu
      versions:
        - jammy
  galaxy_tags:
    - unraid
    - prometheus
